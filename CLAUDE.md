# CLAUDE.md — AI Assistant Guide for firma-

## Project Overview

**firma-** is an application project currently in its initial setup phase. The repository has been initialized with only a placeholder README.

## Repository Structure

```
firma-/
├── README.md          # Project description
└── CLAUDE.md          # This file — guidance for AI assistants
```

## Current State

This project is freshly initialized. There is no source code, build tooling, test framework, or CI/CD pipeline configured yet. As the project develops, this file should be updated to reflect new structure, conventions, and workflows.

## Git Workflow

- **Primary branch:** `master`
- **Remote:** origin
- Commit messages should be clear and descriptive
- Keep commits focused on a single logical change

## Development Setup

No dependencies or build steps are required yet. As tooling is added, document setup instructions here:

```sh
# TODO: Add setup commands as the project evolves
# e.g., npm install, pip install -r requirements.txt, etc.
```

## Testing

No test framework is configured yet. When tests are added, document how to run them here:

```sh
# TODO: Add test commands
```

## Linting and Formatting

No linters or formatters are configured yet. When added, document them here:

```sh
# TODO: Add lint/format commands
```

## Key Conventions

- Keep this CLAUDE.md file updated as the project evolves
- When adding new tooling or frameworks, document the relevant commands and conventions in this file
- Follow the existing code style and patterns established in the codebase

## Notes for AI Assistants

- Always read relevant source files before proposing changes
- Do not over-engineer — keep changes minimal and focused on what is requested
- When the project gains structure (src/, tests/, config files), update this document accordingly
- Check for a `.env.example` or similar before assuming environment variable names
